void main() {
  List P = [1, 2, 3, 4, 5, 6];
  int sum = 0;

  P.forEach((element) {
    print(element);
  });

  // P.forEach((element) {
  //   sum = sum + element;
  // });
  /* // We Can do this too
  P.forEach((i) {
    print(i);
  });
  */
}
